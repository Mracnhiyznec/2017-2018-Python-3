"Функция, которая генерирует все числа (с лидирующими незначащими нулями) в N-ричной системе счисления (N<=10) длины M рекурсивно"
def generate_number(N, M, prefix=None): # задаем функцию с параметрами N(количество чисел, которые перебираем от 0 до N - 1), M(количество мест, куда можем их поставить), prefix лист в котором будут формировать последовательность(по умолчанию None)
  prefix = prefix or [] # листу возвращается он сам, если он не пуст, иначе возвращаем ему пустой лист
  if M == 0:
    print(*prefix)
  else:
    prefix.append(i) # добавляем в лист следущий элемент
    generate.number(N, M - 1, prefix)
    prefix.pop()
  
