#Быстрое возведение в степень(быстрая степень) c помощью рекурсии
def pow(a, n):
  if n == 0:
    return 1 # любое число в нулевой степени равно 1 
  else:
    if (n % 2 != 0):
       return a * pow(a, n - 1) # если степень не делится на a то возврашаем a * a ^ (n - 1) т.е a умноженное на а от предыдущей степени.
    else:
      return pow(a**2, n//2) # если степень делится на a то можем вернуть (a ^ 2) ^ (n / 2) т.е сбрасываем показатель степени сразу в половину, что ускоряет код 
