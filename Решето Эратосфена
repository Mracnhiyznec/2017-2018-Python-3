# Решето Эратосфена, алгоритм поиска простых чисел. Функция возвращает True, если число простое, иначе False
def resheto_eratoshena(x):
  list = [True]*(x + 1) # изначально говорим что все числа простые(в листе по индексу находится True, если число простое, иначе False)
  list[0] = list[1] = False # числа 0 и 1 не простые ==> присваиваем им значение False
  for i in range(2, x + 1): # основной цикл в котором проходим все числа от 2 до x и присваиваем False если число составное, иначе True
    if (list[i]): # Если значение в списке True, то идем циклом от i * 2 до x + 1 с шагом i, которые, очевидно, деляться на i
      for j in range(i * 2, x+1, i): 
        list[j] = False # присваиваем им значения False, так как они составные
  return list[x] Возвращаем булево значение от искомого числа
print(resheto_eratoshena(7))
# Таким образом узнали простоту не только заданного числа, но и всех меньших чисел
