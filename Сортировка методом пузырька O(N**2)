# Сортировка методом пузырька (bubble sort) сложность: O(N ^ 2)
def bubble_sort(list):
  N = len(A) # Присваиваем перемнной длину листа 
  for bypass in range(1, N): # основной цикл, который будет ставить следующий макисмальный элемент на свое место
    for k in range(0, N - bypass): # цикл, который двигает следующий максимум на свое место и каждый раз не доходим до предыдущего максима, чтобы не делать лишних итерациях
      if A[k] > A[k + 1]: # сравнение двух пузырьков 
        A[k], A[k + 1] = A[k + 1], A[k] # меньший пузырек отправляет в начало листа, больший двигаем вправо
  retutn list # возвращаем отсортированный лист
