# Сортировка подсчетом (сount sort), слоность O(N). Данная сортировка годится только если нам известен диапазон входных данных и лучше чтобы они повторялись 
def count_sort(list):
  # например знаем, что максимальное число во входных данных не превосходит N
  K = F[0] * (N + 1) 
  for i in range(K):
    x = int(input())
    F[x] += 1
# на выходе получаем список в котором в элемент с данным идексом будет указано сколько раз он повторялся в массиве и выводим в порядке возрастания.
