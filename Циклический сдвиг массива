# #Циклический сдвиг массива влево (1 шаг)
def left_list(list):
  tmp = list[0] # присваиваем временной переменной первый элемент массива, чтобы не потерять его при сдвиге 
  N = len(list) # присваиваем переменной N длину списка
  for i in range(N - 1): # от нулевого элемента до предпоследнего присваиваем данному элементу следующий.
    list[k] = list[k + 1] # присвоение
  list[N - 1] = tmp # последний элемент должен получить первый из временной переменной tmp 
  return list # возвращаем список поэлементно сдвинутый влево один раз.
# # Циклический сдвиг массива вправо
def right_list(list):
  N = len(list)
  tmp = list[N - 1] # присваиваем переменной N последний элемент массива, что не потерять его при сдвиге
  for k in range(N-2, -1, -1): # начинаем с предпоследнего элмента, чтобы не выйти за границы массива
    A[k + 1] = A[k] # присваиваем следующему элементу прыдыдущий.
  A[O] = tmp # первому элементу присваиваем последний 
