# Генерация всех размещений с повторениями из двух цифр, возвращает строки последовательности.
def gen_bin(M, prefix = ""): # М количество мест куда можем ставить цифры, prefix по умолчанию пустая строка 
  if M == 0:
    print(prefix) # Если дошли до конца строки то возвращаем её
  else:
    gen_bin(M - 1, prefix + "0") # возвращаем ту же функцию от строки размером на единицу меньше и с добавлением сначал "0" так как порядок лексикографический
    gen_bin(M - 1, prefix + "1") # возвращаем ту же функцию от строки размером на единицу меньше и с добавлением сначал "1" так как порядок лексикографический
